import { FilterbesoinPipe } from './filterbesoin.pipe';

describe('FilterbesoinPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbesoinPipe();
    expect(pipe).toBeTruthy();
  });
});
